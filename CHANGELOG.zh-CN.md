# 更新日志

这里记录 ReTheme 桌面端的重要变更。

## [0.4.8] - 2026-08-29

### 修复

- 隔离 ChatGPT 启动增加全局串行锁；自动兼容自检、主题应用和 Wallpaper Engine 应用不再并发打开多个 ChatGPT 窗口。
- 每次应用只允许一次隔离启动尝试，并要求页面连续稳定 8 秒、总启动等待提高到 45 秒，以较慢但更可靠的单窗口启动替代重复拉起。
- 自动兼容自检延迟 8 秒；用户开始应用主题或壁纸时会取消等待中的自检，避免争抢启动窗口。
- 关闭隔离实例时按进程树清理 ChatGPT 子进程，避免测试页面残留。

### 优化

- Scene 捕获间隔由 100 ms 调整为 33 ms，保留 2561×1601 原生尺寸和 JPEG 80；本机原版 Scene 实测从约 10 FPS 提高到 15.0 FPS。
- 选择 Scene 时先把同一项目同步为 Wallpaper Engine 桌面壁纸，再通过离屏窗口捕获纯壁纸；配合 `ChatGPT.exe` 最大化时暂停桌面壁纸的应用规则，ChatGPT 最大化时只让离屏实例持续渲染。
- Scene 帧通过单一 CDP 连接传入页面，跳过没有新画面时的重复编码；停止主题时先结束帧通道，再建立清理连接。
- Windows 正式构建使用官方 NASM 3.02，为 libjpeg-turbo 启用 x64 SIMD 编码路径。

## [0.4.7] - 2026-08-28

### 修复

- Scene 捕获由不兼容 DirectX 壁纸窗口的 GDI `PrintWindow` 改为 Windows Graphics Capture，避免已正常播放的 Wallpaper Engine Scene 在 ChatGPT 中显示纯黑。
- 使用 Wallpaper Engine 的屏幕外原生分辨率播放窗口获取干净画面，不再捕获包含桌面图标的 `Program Manager`，也不再放大 Workshop 低清预览图。
- ReTheme 只复用或正常启动 Wallpaper Engine，不再强制结束其进程，避免由 ReTheme 导致下次启动进入“安全启动”。

### 优化

- 改用 libjpeg-turbo 直接压缩 BGRA 帧，删除逐帧 BGRA→RGB 整帧复制，并只对新捕获帧重新编码。
- 捕获限制为约 10 FPS；实测 2561×1601 Scene 的平均捕获与 JPEG 编码耗时由约 93.7 ms 降至 37.1 ms。
- 删除旧 GDI 画布、DIB 与 XPS 依赖，保留 Video/Web 无需 Wallpaper Engine 的直接播放路径。

### 说明

- 选择 Scene 时仍会把主显示器桌面同步切换为同一项目。Wallpaper Engine 的桌面 DirectX 子窗口无法被公开捕获 API 单独干净抓取，因此 ChatGPT 使用隐藏的屏幕外捕获窗口；没有采用隐藏桌面图标、进程注入或关闭安全功能等高风险方案。

## [0.4.6] - 2026-08-27

### 修复

- 不再把仅存在但持续输出纯黑的 Wallpaper Engine 桌面窗口视为成功；应用 Scene 前必须确认真实首帧已经渲染。
- Wallpaper Engine 已运行时直接复用现有实例，切换 Scene 后主动恢复播放，避免重复启动和停止状态导致黑屏。
- 运行中遇到短暂纯黑帧时保留上一张有效画面，不再把 ChatGPT 背景刷新为黑色。
- 安全启动尚未完成时显示可操作的错误说明，不会自动关闭 Wallpaper Engine 的“防止崩溃”保护。

### 验证

- 新增纯黑帧识别测试；安全启动状态下不再创建黑屏主题会话，正常 Scene 仍使用单个桌面渲染实例。

## [0.4.5] - 2026-08-27

### 调整

- 选择原版 Scene 后，会把 Wallpaper Engine 主显示器桌面同步切换为同一项目，并直接抓取已有桌面渲染器，不再额外启动第二份 `playInWindow`。
- 仅当无法识别 Wallpaper Engine 桌面窗口时，才自动回退到独立屏幕外渲染，避免不兼容设备完全无法应用壁纸。
- 在 Scene 分类中直接说明应用后会替换主显示器桌面壁纸并复用同一渲染实例。

### 验证

- 两个 Workshop Scene 始终只存在一个 `WPEDesktop… / WPELiveWallpaper` 渲染窗口，没有创建 `ReThemeWEScene`；实测 14.3–14.5 FPS、0 次传输错误，暂停与恢复均正常。

## [0.4.4] - 2026-08-27

### 优化

- Scene 抓帧复用 GDI 画布与 RGB 缓冲区，不再为每一帧重新分配整套 1920×1080 抓取资源；实测 Workshop Scene 的原始抓帧平均耗时由 36.9 ms 降至 28.7 ms。
- 使用一条持续授权的 DevTools 连接传输内存 JPEG 帧，删除逐帧临时文件写入、文件输入注入、Blob URL 替换和主题 DOM 更新。
- 页面改用 Canvas 解码；页面忙碌时只保留最新待处理帧，避免旧帧越积越多。两个实测 Scene 的播放速度由约 10 FPS 提升到 12.8–15.0 FPS。

### 验证

- 两个 Wallpaper Engine 原版 Scene 均无传输错误；暂停后帧序号保持不变，恢复后继续推进。

## [0.4.3] - 2026-08-27

### 修复

- ChatGPT 冷启动时不再等待输入框完整加载后才安装主题运行时；应用壳层就绪后先挂载壁纸和 DOM 观察器，避免误报 `view:home|home-compact|conversation|other` 缺失或重试时出现 `os error 10060`。
- 输入框、侧栏和会话区域稍后出现时会由观察器继续补齐主题标记，正常主页和旧对话的透明化逻辑保持不变。

## [0.4.2] - 2026-08-26

### 修复

- Scene 项目恢复使用 Wallpaper Engine 对 `scene.pkg` 的原版渲染，不再把 256×256 Workshop 预览图放大为全屏背景。
- Scene 以 1920×1080、JPEG 质量 90 输出，并通过浏览器本地文件句柄和可撤销 Blob URL 传入 ChatGPT；不再使用会被页面策略拦截的回环图片流，也避免旧逐帧 Base64 调试通道的额外负担。

### 调整

- 所有 Scene 项目重新分类到“需要打开 Wallpaper Engine”；Video 与受限沙箱 Web 仍归入“无需打开 Wallpaper Engine”。
- Scene 同样显示播放/暂停按钮；暂停后冻结传输帧并停止重复抓取与编码；播放时只替换背景图片，不再逐帧重排整套主题界面。

## [0.4.1] - 2026-08-26

### 修复

- 通过浏览器本地文件句柄和可撤销 Blob URL 绑定 Scene GIF/JPG，兼容 ChatGPT 的图片内容安全策略，确保轻量预览实际解码显示。
- 页面重载或租约恢复后会重新绑定本地 Scene 文件，不会退回被页面安全策略拦截的回环 URL。

## [0.4.0] - 2026-08-26

### 新增

- Scene 壁纸默认使用 Workshop 自带的 GIF/JPG 轻量预览，无需启动 Wallpaper Engine；GIF 直接播放，静态图使用低成本缓慢平移缩放。
- 主题库按后端返回的实际引擎依赖分类，轻量 Scene 会显示在“无需打开 Wallpaper Engine”分组。

### 优化

- 删除 Scene 离屏窗口、GDI 截图、逐帧 JPEG 编码、Base64 传输与调试通道帧桥接，降低 CPU、GPU、内存和卡顿。
- 轻量模式省略原 Scene 的粒子、音频、鼠标交互和脚本效果；Video 与沙箱 Web 播放方式保持不变。

## [0.3.9] - 2026-08-25

### 修复

- 将新增的动态壁纸播放按钮类加入主题 CSS 安全白名单，避免 0.3.8 应用壁纸时被选择器校验拒绝。

## [0.3.8] - 2026-08-25

### 新增

- 在 ChatGPT 右下角的 ReTheme 动态壁纸控制面板中增加播放/暂停按钮。
- 暂停状态会同时停用自动恢复播放保护，避免视频在按下暂停后一秒内自行恢复。

## [0.3.7] - 2026-08-25

### 修复

- 修正 0.3.6 底部渐变扫描中的注入脚本变量作用域错误，确保动态壁纸能正常启动并应用黑框修复。

## [0.3.6] - 2026-08-25

### 修复

- 识别新版 ChatGPT 位于对话滚动区内部的底部 `from-surface` 渐变层，不再只检查输入区的同级元素。
- 同时移除旧对话的滚动区底部渐变和输入框上方的小渐变，彻底清除输入区下方的矩形黑框。

## [0.3.5] - 2026-08-25

### 修复

- 仅在旧对话/对话视图中移除主内容区的深色背景，不影响主页、设置页、侧栏和输入框透明度。
- 兼容新版 ChatGPT 的 `_MainContentFrame_*` 容器并清除顶部边框，避免旧对话外围再次出现黑框。

## [0.3.4] - 2026-08-25

### 修复

- 兼容 ChatGPT 更新后的输入区 DOM 结构，识别贴底的 `relative`、`absolute`、`fixed` 和 `sticky` 容器。
- 清除底部输入区容器及伪元素的背景、渐变、模糊和阴影，避免输入框下方再次出现大块黑影。

## [0.3.3] - 2026-08-25

### 修复

- 将两个 Wallpaper Engine 分类选择区移到独立整行，避免分类标题因空间不足被省略。

## [0.3.2] - 2026-08-25

### 修复

- 将藏在原生长下拉框底部的分类改成两个始终可见的独立选择区，分别显示“无需打开 Wallpaper Engine”和“需要打开 Wallpaper Engine”及各自项目数量。

## [0.3.1] - 2026-08-25

### 调整

- Wallpaper Engine 项目选择器分为“无需打开 Wallpaper Engine（Video / Web）”和“需要打开 Wallpaper Engine（Scene）”两组。

### 修复

- 移除 ChatGPT 输入框向下延伸的大面积黑色投影，同时保留输入框背景透明度及其他卡片阴影。

## [0.3.0] - 2026-08-25

### 新增

- 支持 Wallpaper Engine 的 Scene 和 Web 项目，不再把这两类 Workshop 壁纸标为“未执行”。
- Scene 项目由本机已安装的 Wallpaper Engine 官方程序渲染，并通过 ReTheme 的本机主题通道送入 ChatGPT 背景层。
- Web 项目在仅允许脚本的隔离 iframe 中运行，同时禁止联网、弹窗、表单、下载、跨源访问及对象嵌入。

### 调整

- 主题库现在分别显示 Video、Scene、Web 的数量；当前 Workshop 中 24 个 Video、33 个 Scene 和 2 个 Web 项目均可选择。
- 停止或切换 Scene 壁纸时仅关闭 ReTheme 创建的专用渲染窗口，不退出 Wallpaper Engine 主程序。

## [0.2.3] - 2026-08-24

### 新增

- 在 ChatGPT 右下角加入可收起的“壁纸亮度”和“界面透明度”双滑块，拖动后实时生效。
- 将双滑块数值保存到本机，关闭并重新开启 ReTheme 后自动恢复上次设置。

### 修复

- 彻底移除首页 Wallpaper Engine 说明区域及其残留黑色空框，而不是只隐藏其中的文字。
- 修复旧透明度选项只影响壁纸视频且必须重新应用才生效的问题；新版分别控制壁纸亮度和界面遮罩透明度。
- ChatGPT 在应用后替换页面目标时自动重新注入主题、重新挂载动态视频并恢复当前调节值。

## [0.2.2] - 2026-08-24

### 新增

- 新增 Wallpaper Engine 背景透明度调节，并在管理窗口或应用重开后保留上次选择的 Workshop 壁纸和透明度。

### 调整

- 隐藏 ChatGPT 主页壁纸横幅中的 Wallpaper Engine 来源、壁纸标题与流式播放说明，同时保持原有页面布局。

## [0.2.1] - 2026-08-24

### 修复

- 兼容 ChatGPT 26.818.5229.0 的启动时序：等待应用页面真实完成导航并短暂稳定后再连接本地主题通道，避免动态壁纸应用超时。
- 长诊断信息不再把“运行诊断”按钮挤出可见区域。

## [0.2.0] - 2026-08-22

### 新增

- 支持从 Wallpaper Engine Workshop 扫描并流式播放视频壁纸，不再受 8MB 图片导入限制。
- 新增按 ChatGPT 版本自动运行并缓存结果的兼容自检，支持 Windows 四段式包版本。
- 新增可配置的 GitHub Releases 自动更新，只接受 ReTheme WE 独立密钥签名的安装包。
- 新增默认开启的低内存模式：应用主题后销毁管理 WebView，托盘后端继续运行并可随时重建窗口。

### 清理

- 移除未使用的前端 updater 依赖、权限以及与 Windows 分支无关的发布工作流。

## [0.1.4] - 2026-07-20

### 新增

- 概览页新增 GitHub Star 支持入口，方便用户前往支持开源项目。

### 修复

- 修复 Windows Microsoft Store/MSIX 版 ChatGPT 位于 `WindowsApps` 时无法应用主题的问题。
- 通过系统 AppUserModelId 激活隔离 ChatGPT，并精确回收本次激活的进程。

## [0.1.3] - 2026-07-19

### 新增

- 受控主题资源支持声明 `lightAsset` 与 `darkAsset`，跟随 ChatGPT 明暗模式原位切换，缺少对应变体时回退到 `asset`。
- 桌面加载、主题开发 Skill 与服务端社区审核共用同一套明暗资源协议。

## [0.1.2] - 2026-07-19

### 新增

- 新增桌面加载、本地开发、AI 工作流和服务端社区审核共用的 Rust 主题协议与 CLI 校验器。
- 补全 v1 Manifest Schema、164 个稳定插槽、带注释示例、Banner 素材规格、生图提示词和可复用主题开发 Skill。

### 修复

- 在首页输入框编辑时只刷新受影响的首页插槽，避免完整重注入引起的布局闪动。
- 替换或恢复主题运行时时，完整释放观察器与动画帧。

### 安全

- 通过共享协议统一拒绝未知 Manifest 字段、不安全压缩路径、越界或结构性 CSS、外部资源、无效图片内容和不安全 SVG。

## [0.1.1] - 2026-07-19

### 修复

- 修复 Windows 重新安装 ReTheme 后旧设备加密缓存无法打开，导致已安装主题不可用的问题。
- 手动检查更新后显示明确结果，包括 ReTheme 已是最新版本的情况。
- 修复 Tag 发布时应用版本与更新清单未同步到标签版本的问题。

## [0.1.0] - 2026-07-19

### 新增

- 支持已安装主题管理、社区下载、本地主题开发、账号状态、云端收藏与设备记录。
- 通过本地 Rust 运行时应用签名主题，并按 ChatGPT 版本获取远程兼容数据。
- 支持简体中文与英文、托盘、深链、深浅色外观和签名自动更新。
- 提供 macOS Apple Silicon、macOS Intel 与 Windows x64 签名安装包。

### 安全

- 使用前验证主题包与兼容数据，主题资源仅通过受限的本机回环会话提供。
- 退出 ReTheme 或安装应用更新前自动恢复 ChatGPT 官方界面。
